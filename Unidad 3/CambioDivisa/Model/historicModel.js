export class Historico {
  constructor(valueOrigin, valueExchange, divisaFrom, divisaTo, fechaActual) {
    this.valueOrigin = valueOrigin;
    this.valueExchange = valueExchange;
    this.divisaFrom = divisaFrom;
    this.divisaTo = divisaTo;
    this.fechaActual = fechaActual;
  }
}
